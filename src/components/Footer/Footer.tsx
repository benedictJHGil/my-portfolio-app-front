import styles from "./Footer.module.css"

function Footer() {
    return (
        <footer className={styles.footer}>
            <p>Designed by Benedict Gil</p>
        </footer>
    )
}

export default Footer;